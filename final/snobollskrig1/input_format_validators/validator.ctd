INT(2, 100000, n) SPACE INT(2, 50, l) SPACE INT(1, 100000, m) NEWLINE

REPI(i, l)
	INT(0,n-1,fort[i]) NEWLINE
END
ASSERT(UNIQUE(fort))

REPI(i, m)
	INT(0,n-1,a[i]) SPACE INT(0,n-1,b[i]) SPACE INT(1,2000) NEWLINE
	ASSERT(a[i] != b[i])
END
ASSERT(UNIQUE(a, b))
EOF
