INT(2, 2000, n) SPACE INT(1, 1000000, m) SPACE INT(0, n-1, s) SPACE INT(0, n-1, p)
NEWLINE

REP(m)
	INT(1, n, a) SPACE INT(1, n, b) NEWLINE
	ASSERT(a != b)
END

REPI(i, s)
	INT(1, n - 1, c[i]) NEWLINE
	ASSERT(UNIQUE(c))
END

EOF
